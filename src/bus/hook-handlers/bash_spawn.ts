// TODO: Day-3 Codex pair lands the dispatch logic
import type { HandlerFn } from '../hooks';

export const bashSpawnHandler: HandlerFn = () => {
  throw new Error('TODO: bash_spawn dispatch logic — Codex pair Day-3');
};
