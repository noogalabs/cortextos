// TODO: Day-3 Codex pair lands the dispatch logic
import type { HandlerFn } from '../hooks';

export const webhookFetchHandler: HandlerFn = () => {
  throw new Error('TODO: webhook_fetch dispatch logic — Codex pair Day-3');
};
